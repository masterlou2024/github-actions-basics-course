name: hello world

on:
  workflow_dispatch:
    branches:
      - 1_hello_world_start

jobs:
  print-hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello World"
        shell: bash
